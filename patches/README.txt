The public H63 IPS is intentionally generated from the private clean NACE WAD and private tested H63 reference WAD.

Run OoTVCFixPatcher.py, then use:
Maintainer -> Build H63 patch payload

After generation, this directory will contain:
oot-vc-usa-h63.ips

That IPS is the only patch payload the public EXE needs.
